// Sign-up currently skips the org picker and auto-joins this org.
// Change this (or bring back org selection in sign-up-form.tsx / auth.ts) when
// this app needs to support onboarding into more than one organization.
export const DEFAULT_ORG_CODE = "T-62850";
